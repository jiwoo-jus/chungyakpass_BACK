package com.hanium.chungyakpassback.repository.standard;

import com.hanium.chungyakpassback.domain.standard.월평균소득기준;
import org.springframework.data.jpa.repository.JpaRepository;

public interface 월평균소득기준repository extends JpaRepository<월평균소득기준, Long> {
}
